export default function Home() {
  return (
    <div style={{ padding: '4rem', fontFamily: 'monospace', fontSize: '1.2rem', color: '#ccc' }}>
      <h1>This file (app/page.tsx) is currently not displayed.</h1>
      <p>The active homepage for this project is located in the `pages/index.tsx` file because both `pages` and `app` directories exist.</p>
    </div>
  );
}
